#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main()
{
    char str[20];
    printf("enter the string : ");
    scanf("%s", str);
    int a =strcmp()    printf("original string : %s\n", str);
    for (int i = a; i >= 0; i--)
    {
        printf("%c", str[i]);
    }
    return 0;
}